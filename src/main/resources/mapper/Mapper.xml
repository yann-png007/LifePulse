<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lifepulse.mapper.MerchantMapper">

    <select id="selectByCategoryId" resultType="com.lifepulse.entity.Merchant">
        SELECT * FROM lp_merchant WHERE category_id = #{categoryId} AND status = 1
    </select>

    <select id="selectHotPage" resultType="com.lifepulse.entity.Merchant">
        SELECT * FROM lp_merchant WHERE hot_flag = 1 AND is_deleted = 0 LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectColdPage" resultType="com.lifepulse.entity.Merchant">
        SELECT * FROM lp_merchant WHERE hot_flag = 0 AND is_deleted = 0 LIMIT #{offset}, #{pageSize}
    </select>

    <update id="batchUpdateToCold">
        UPDATE lp_merchant
        SET hot_flag = 0, update_time = NOW()
        WHERE id IN
        <foreach collection="idList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
</mapper>